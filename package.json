{
  "name": "nova-site",
  "version": "1.0.0",
  "private": true,
  "description": "Nova (NOVA) landing page - static site",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
